# Core dependencies for ExamGenerator library
reportlab>=4.0.0
Pillow>=10.0.0  # Note: Capital P for consistency
qrcode[pil]>=7.0.0

# Encryption support for password-protected PDFs
# cryptography is automatically installed as reportlab dependency
